# Partial configuration for Azure Remote State.
# The actual values are provided at init time via backend.hcl
# Command: terraform init -backend-config=backend.hcl

terraform {
  # backend "azurerm" {}
}