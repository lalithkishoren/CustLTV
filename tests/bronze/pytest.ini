[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: Unit tests for specific transformations
    integration: End-to-end notebook execution tests
    idempotency: Tests ensuring safe re-runs
    data_quality: Tests for schema and data contract enforcement
filterwarnings =
    ignore::DeprecationWarning